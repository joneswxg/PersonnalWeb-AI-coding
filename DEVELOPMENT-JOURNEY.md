# 从一句话到上线网站：个人学习记录博客开发全记录

这份文档记录了「个人学习记录博客」项目从最初一句话的想法，到最终在生产环境跑起来的完整过程。目的是留一份可以回头复习的学习资料——既包括做了什么，也包括为什么这么做、踩过什么坑。

整个过程用的是 **OpenSpec 的 spec-driven 工作流**：先想清楚（explore）→ 写成规格文档（propose）→ 按规格实现（apply）。这不是瞎写代码，而是先把"要做什么、为什么做、怎么做"想透彻，落成文档，再动手写代码。

---

## 第一阶段：需求探索（`/opsx:explore`）

起点只有一句话：

> 帮我设计一个个人网站，类似 blog，用来记录每天学习的内容

如果直接开始写代码，大概率会做出一个"看起来像博客"但跟真实需求不匹配的东西。所以第一步不是写代码，而是**用一连串问题把模糊的想法逐步收紧成具体的产品决策**。这个阶段的关键产出不是文件，而是一系列决策：

| 探索的问题 | 最终决策 |
|---|---|
| 给谁看？ | 主要给自己复盘，但支持部分内容公开 |
| 内容形态？ | 偶发的正式文章（不是每日流水账打卡，不需要连续打卡热力图） |
| 内容组织方式？ | 层级分类（一篇一类）+ 自由标签（多对多） |
| 权限怎么分？ | 状态机 `draft → private → public`，角色 `访客/信任用户/owner` |
| 登录方式？ | GitHub OAuth（不自建用户名密码系统） |
| 写作方式？ | 后台在线 Markdown 编辑器，数据库是唯一数据源（不走本地文件同步） |
| 搜索要求？ | 必须服务端做、按权限过滤——明确排除"生成静态搜索索引发给浏览器"这种会泄露私密内容的方案 |
| 部署方式？ | GitHub 仓库 + 零运维托管平台（Vercel/Neon 这类），免费额度内跑起来 |
| 视觉风格？ | 卡片式布局 |

**这一阶段最大的价值**：把"大部分文章是私密的"这个信息提前问出来，直接否决了"纯静态站点 + 静态搜索索引"这条看似简单但实际上会泄露隐私内容的路线，逼着架构往"有服务端鉴权"的方向走。如果跳过这一步直接开始写代码，很可能走了静态站点这条路之后才发现做不了权限控制，要推倒重来。

---

## 第二阶段：生成规格文档（`/opsx:propose`）

把探索阶段的决策，转成一份结构化、可执行的规格。OpenSpec 的 spec-driven 模式下，一个 change 包含四类文件：

```
openspec/changes/personal-learning-blog/
├── proposal.md      ← 为什么做、做什么、影响哪些能力
├── design.md        ← 技术怎么实现、为什么选这个技术栈
├── specs/           ← 每个能力的具体行为规格（可测试）
│   ├── access-control/spec.md
│   ├── article-management/spec.md
│   ├── content-browsing/spec.md
│   └── content-search/spec.md
└── tasks.md         ← 拆解成可勾选的实施任务清单
```

### `proposal.md`：为什么 & 做什么

定义了 4 个新的"能力"（capability），每个能力后面都会对应一份独立的 spec 文件：

- **access-control**：GitHub OAuth 登录 + 角色模型 + 信任名单管理
- **article-management**：后台写作、分类标签、发布状态机
- **content-browsing**：前台浏览、卡片列表、分类/标签导航、文章详情
- **content-search**：服务端全文搜索

### `design.md`：技术选型与理由

这是整个项目的技术决策记录，每个选择都带着"为什么选它、放弃了什么"：

| 决策 | 选择 | 为什么 |
|---|---|---|
| 框架 | Next.js（App Router）+ Vercel | 一个应用同时承载前台和后台，Server Components 天然适合做权限过滤渲染；Vercel 对 Next.js 支持最原生，GitHub push 自动部署 |
| 数据库 | Neon（Serverless Postgres）+ Drizzle ORM | 零安装托管数据库，免费额度够用；选 Postgres 而不是 SQLite，是因为要用它的原生全文搜索能力 |
| 搜索 | Postgres `tsvector`/`tsquery`，服务端查询 | 权限过滤和全文匹配在同一条 SQL 里完成，天然不会把内容索引发到客户端 |
| 登录 | Auth.js + GitHub Provider | 不用自己造用户名密码系统，访问者用自己的 GitHub 账号登录 |
| 写作 | 后台在线 Markdown 编辑器 | 数据库是唯一数据源，不会出现"本地文件"和"数据库"两份数据不同步的问题 |
| UI | Tailwind CSS + shadcn/ui，卡片式布局 | 快速搭出干净一致的视觉风格 |

`design.md` 里还留了几个"开放问题"（比如图片上传是否要支持、标签管理要不要单独的后台界面），这些不阻塞开始实施，可以先用合理默认值推进，遇到再决定。

### `specs/*.md`：每个能力的具体行为规格

这是最关键的一类文件——把"系统应该怎么表现"写成**可测试的场景**，格式是 `### Requirement` + `#### Scenario`，每条都是 `WHEN ... THEN ...`。

举个例子，`content-browsing/spec.md` 里这条规格：

```markdown
### Requirement: Role-Filtered Card Listing
The system SHALL render article listings as a card-based grid,
showing only articles whose status is permitted for the viewer's role.

#### Scenario: Visitor views the listing
- WHEN a request with role `visitor` views an article listing
- THEN the system shows only `public` articles
```

这种写法的好处是：**规格本身就是测试用例的雏形**。后面写自动化测试时，几乎是把这些场景直接翻译成代码。四份 spec 文件总共定义了十几个 Requirement，覆盖了登录鉴权、文章状态机、卡片浏览、全文搜索这四块能力的所有行为边界。

### `tasks.md`：拆解成可执行任务

把以上规格拆解成 **7 个分组、共 39 个可勾选的具体任务**，按依赖顺序排列：

```
1. 项目搭建（Next.js / Tailwind / Drizzle / 环境变量）
2. 数据模型与迁移
3. 权限能力（access-control）
4. 文章管理能力（article-management）
5. 内容浏览能力（content-browsing）
6. 内容搜索能力（content-search）
7. 部署与验证
```

---

## 第三阶段：按规格实施（`/opsx:apply`）

逐个任务实现，每完成一个就在 `tasks.md` 里打勾。这里挑几个有学习价值的细节说。

### 1. 数据模型：用 Postgres 原生能力做全文搜索

```typescript
searchVector: tsvector("search_vector").generatedAlwaysAs(
  () => sql`to_tsvector('english', coalesce(title,'') || ' ' || coalesce(content,''))`,
),
```

这是一个**数据库自动维护的生成列**：每次 `articles` 表的 `title`/`content` 变化，Postgres 自动重新计算这一列的分词索引，不需要应用代码手动同步。配合 GIN 索引，搜索就是一条普通 SQL：

```sql
WHERE search_vector @@ plainto_tsquery('english', '关键词')
  AND status IN ('public', 'private')  -- 权限过滤跟全文匹配在同一条查询里
```

这正好实现了探索阶段定的硬性要求：搜索结果天然按权限过滤，不存在"先生成全量索引再过滤"这种会泄露内容的中间状态。

### 2. 权限逻辑：把"判断规则"集中到一个文件里

整个项目里，"谁能看到什么状态的文章"这条规则只在一个地方定义（`src/lib/visibility.ts`），列表页、详情页、搜索全部调用它：

```typescript
export function canViewStatus(role: Role, status: ArticleStatus): boolean {
  if (status === "draft") return false;   // draft 任何角色都看不到（除了后台编辑器）
  if (role === "owner" || role === "trusted") return true;
  return status === "public";
}
```

**这里有个真实踩过的坑**：最初的实现里，`owner` 角色被写成"什么状态都能看到"，包括 `draft`。写集成测试时（针对 visitor/trusted/owner × draft/private/public 九种组合做穷举测试），测试断言"任何角色在公开页面都不应该看到 draft"，结果 `owner` 这一项测试失败——因为代码逻辑允许 owner 在前台页面看到草稿，但产品需求其实是"草稿只能在后台编辑器里看到，前台谁都不该看到，包括 owner 自己"。

> **学到的经验**：穷举式的矩阵测试（把所有角色 × 所有状态的组合都测一遍）能抓到这种"看起来合理但跟需求有偏差"的逻辑错误，光靠人工想象很容易漏掉边界情况。

### 3. 测试驱动验证

整个实现阶段写了 48 个自动化测试，包括：
- 纯函数单元测试（角色解析、slug 生成、标签去重等）
- 针对真实本地 Postgres 跑的集成测试（穷举权限矩阵、全文搜索过滤）

这些测试不是写完代码之后补的，而是**和功能同步写、同步验证**——每完成一个任务分组，就跑一次完整测试套件确认没有破坏之前的功能。

---

## 第四阶段：部署上线

这一阶段的特点是：**很多步骤必须用户亲自操作**（注册账号、网页点击、输入密码），AI 能做的是引导、检查、补充自动化的那部分。也是这个项目里踩坑最多的阶段。

### 部署架构：GitHub + Vercel + Neon 三件套

```
┌─────────────────────────────────────────────────────────┐
│              GitHub 仓库（源代码 + CI 触发）                │
│         joneswxg/PersonnalWeb-AI-coding · main 分支       │
└───────────────────────┬───────────────────────────────────┘
                        │ git push 自动触发
                        ▼
┌─────────────────────────────────────────────────────────┐
│        Vercel（托管 + 自动部署 + Serverless 函数）           │
│                                                           │
│  Next.js App Router 渲染：                                │
│   - 公开页面（首页/分类/标签/文章详情/搜索）— Server Component │
│   - 管理后台（/admin/*）— 角色校验后才放行                    │
│   - API 路由（/api/auth/*）— Auth.js 处理 GitHub OAuth 回调  │
└───────────────────────┬───────────────────────────────────┘
                        │ 每次请求查询/写入
                        ▼
┌─────────────────────────────────────────────────────────┐
│         Neon（Serverless Postgres，按需伸缩，免费档）         │
│   articles / categories / tags / article_tags /          │
│   trusted_users                                          │
└─────────────────────────────────────────────────────────┘
```

三个服务各司其职、互不重叠：GitHub 管代码和触发部署，Vercel 管运行和路由，Neon 管数据持久化。三者都有免费额度，个人博客这种流量级别基本不会产生费用。

### 部署过程中的几个关键节点

**1. 凭证安全处理**

部署过程涉及大量密钥：GitHub Personal Access Token、Neon 数据库密码、GitHub OAuth Client Secret、Auth 加密密钥。这里有几条贯穿始终的原则：

- **密钥不能出现在会被记录或长期保存的地方**——比如 AI 工具的命令行参数（会进入进程列表和会话记录）、`/tmp` 这种全局可读目录。多次尝试把密钥直接写进命令行都被工具自带的安全检测拦截了，最终改成"让用户在自己的终端里手动输入一次"，密钥就不会经过 AI 工具这一层。
- **本地的 AI 会话记录目录（`.omc/`）被加进了 `.gitignore`**——因为这次对话本身包含了明文密钥，如果会话记录被提交进公开仓库，密钥就永久留在 git 历史里了。
- **明文聊过的密钥，事后建议轮换**——GitHub token、数据库密码一旦在聊天记录里出现过，就不再算"安全保管"，即使没有实际泄露，最佳实践也是重新生成。

**2. 一个隐蔽但严重的部署 bug：Framework Preset 配置错误**

第一次部署完，访问首页是 `404: NOT_FOUND`，但代码构建日志显示"编译成功"——这种"构建成功但访问失败"的情况最容易让人误判是代码问题。排查后发现：

```
Framework Preset      Other          ← 应该是 Next.js
Output Directory      `public` 目录   ← 应该是 Next.js 的标准输出
```

Vercel 把这个项目识别成了"静态站点"，于是只发布了 `public/` 文件夹（里面只有几张图标），完全没有部署 Next.js 真正构建出来的页面和接口。这是项目最早导入 Vercel 时框架自动识别出错导致的，跟代码本身完全无关。在 Vercel 项目设置里手动把 Framework Preset 改成 Next.js，重新部署后问题消失。

> **学到的经验**：「构建成功」≠「部署成功」≠「线上能访问」，这是三件独立的事，必须逐一验证，不能因为前一步通过就假设后面也没问题。

**3. 环境变量配置容易漏**

Vercel 的环境变量改动**不会自动应用到已经部署的版本**，必须手动 Redeploy 一次才生效——这是个容易被忽略的细节，第一次部署就因为漏配 `AUTH_SECRET` 导致功能不完整。

**4. GitHub 的两种"App"容易混淆**

注册登录用的 OAuth 凴证时，容易走错路径进到"GitHub Apps"（这是给机器人/集成工具用的，有 Webhook URL 等一堆跟登录无关的配置），正确的应该是更简单的"OAuth Apps"，只需要 Homepage URL 和 Authorization callback URL 两项。

### 部署完成后的验证清单

最后通过用户亲自登录验证了：

- [x] GitHub OAuth 登录成功
- [x] 登录后被正确识别为 `owner` 角色（看到了后台管理入口）
- [x] 文章三态（`draft`/`private`/`public`）切换正常
- [x] 未登录访客只能看到 `public` 文章

`trusted` 角色的信任名单流程和完整的角色×状态矩阵没有用第二个 GitHub 账号在生产环境人工跑一遍，但这部分逻辑已经被自动化测试（单元测试 + 针对真实数据库的集成测试）覆盖。**人工验证和自动化测试覆盖的是同一套规则，互为补充，不必每个角度都重复验证。**

---

## 网站最终架构总览

```
                          访客 / 信任用户 / Owner
                                  │
                                  ▼
                    GitHub OAuth 登录（Auth.js）
                                  │
                         角色解析（每次请求）
              ┌──────────────────┼──────────────────┐
              ▼                  ▼                  ▼
          visitor             trusted              owner
       （只看 public）   （public + private）  （全部 + 后台管理权限）
              │                  │                  │
              └──────────────────┼──────────────────┘
                                  ▼
                    ┌─────────────────────────┐
                    │   统一的可见性判断逻辑     │
                    │   canViewStatus()        │
                    │   （列表/详情/搜索都用它） │
                    └────────────┬────────────┘
                                  ▼
        ┌─────────────────────────────────────────────┐
        │              Next.js App Router               │
        │                                               │
        │  前台：首页 / 分类页 / 标签页 / 文章详情 / 搜索    │
        │  后台：文章编辑器（Markdown + 实时预览）/        │
        │        发布状态切换 / 信任名单管理               │
        └────────────────────┬──────────────────────────┘
                             ▼
                  Drizzle ORM（类型安全的查询层）
                             ▼
        ┌─────────────────────────────────────────────┐
        │       Neon Postgres（生产）/ Docker（本地）     │
        │                                               │
        │  articles（含生成列 tsvector + GIN 索引）       │
        │  categories · tags · article_tags · trusted_users │
        └─────────────────────────────────────────────┘
```

**贯穿整个架构的核心设计原则**：可见性规则只在一处定义（`visibility.ts`），所有读路径（列表、详情、搜索）都复用它，避免同一条规则在多处实现导致互相"打架"或者漏改一处出现权限漏洞。`draft` 状态被严格限制为"只能通过后台编辑器看到"，这条规则不依赖角色判断，而是在共享逻辑里直接硬编码排除，杜绝了"owner 角色不小心被允许在公开页面看到草稿"这类逻辑漏洞。

---

## 这次开发过程值得带走的几条经验

1. **先想清楚再动手**：用一轮高质量的提问把模糊需求收紧成具体决策，能在写代码之前就排除掉根本不可行的架构方向（比如静态搜索索引 vs 隐私内容的冲突）。
2. **规格文档即测试用例**：用 `WHEN ... THEN ...` 格式写需求，后面写测试几乎是直接翻译，规格和代码不容易脱节。
3. **穷举式测试能抓住"看起来对但实际错"的逻辑**：单点测试容易漏掉边界组合，把角色 × 状态这种维度做笛卡尔积式的全覆盖测试，能在上线前抓到权限漏洞。
4. **构建成功不代表部署成功，部署成功不代表配置正确**：这是三件独立的事，Framework Preset 配置错误就是典型例子——代码完全没问题，但平台侧的一个设置项导致整个站点不可用。
5. **密钥安全是贯穿全程的纪律，不是部署完才考虑的事**：命令行参数、临时文件、会话日志，都可能变成密钥的泄露面，越早建立"敏感操作交给用户自己在终端完成"的习惯，风险越小。
6. **免费托管栈对个人项目完全够用**：GitHub（代码+触发）+ Vercel（运行+部署）+ Neon（数据库）这套组合，对个人博客级别的流量，可以做到接近零运维成本，零服务器管理。
